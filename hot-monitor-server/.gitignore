# 依赖目录：通过 npm install 生成，不提交到仓库。
node_modules/

# 本地环境变量和敏感配置：实际运行配置不应提交。
.env
.env.*
# 保留可共享的环境变量模板，便于克隆项目后初始化。
!.env.example

# 构建产物和测试覆盖率报告：可在本地重新生成。
dist/
coverage/

# npm 与应用运行日志：避免将本地调试输出提交到仓库。
npm-debug.log*
*.log

# Prisma 本地开发数据库：仅供本机使用，不作为项目数据提交。
prisma/dev.db
